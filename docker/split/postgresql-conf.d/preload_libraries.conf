# Shared libraries loaded at server start
shared_preload_libraries = 'pg_stat_statements, pg_cron'

# Per-session library for query resource limits
session_preload_libraries = 'libquery_supervisor.so'
