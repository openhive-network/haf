variables:
  # pin to specific docker images for build repeatability
  # Uses common-ci-configuration ci-base-image (includes sccache for distributed caching)
  # registry.gitlab.syncad.com/hive/common-ci-configuration/ci-base-image:ubuntu24.04-py3.14-5
  TEST_HAF_IMAGE_TAG: ":ubuntu24.04-py3.14-5"
