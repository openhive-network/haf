variables:
  # pin to specific docker images for build repeatability
  # Using common-ci-configuration ci-base-image (consolidated from hive)
  # uses registry.gitlab.syncad.com/hive/common-ci-configuration/ci-base-image:ubuntu24.04-py3.14-5
  TEST_HAF_IMAGE_TAG: ":ubuntu24.04-py3.14-5"
